fn ret_o() -> int {
    let code: int = 0;
    ret code;
}

fn main() -> int {
    var a: int = 64;

    a = a / 2 - 16 * 2;
    let b: int = a + 1;

    let ret_val: int = b + (a - 10);

    if ret_val != 0 {
        ret_val = ret_val;
    } else {
        ret_val = ret_val + ((8 - 3) * 2) - 1;
    }

    ret ret_o();
}