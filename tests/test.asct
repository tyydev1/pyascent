fn main() -> int {
    var a: int = 64;

    a = a / 2 - 16 * 2;
    let b: int = a + 1;

    let ret_val: int = b + (a - 10);

    if ret_val != 0 {
        ret ret_val;
    } else {
        ret_val = ret_val + ((8 - 3) * 2) - 1;
    }

    ret ret_val;
}